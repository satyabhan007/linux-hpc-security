# Part 3 · labs — runnable configs

> Standard tools, at production depth. Every file here is validated in CI
> (`.github/workflows/lab-tests.yml`).

Landing alongside chapters 2–16: a Slurm partition/QOS/fairshare config
example (`slurm.conf` + `sacctmgr` seed script), a Warewulf node-set/overlay
template, an MPI placement benchmark script (`--cpu-bind`/`--distribution`
sweep), a Node Health Check (NHC) rule set catching GPU Xid errors, and a
Prometheus Slurm job-efficiency exporter config.

Until then, this directory is intentionally a placeholder so links resolve.
